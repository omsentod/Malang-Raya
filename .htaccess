<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect all traffic to the public/ folder with leading slash (required by Hostinger/shared hosting)
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ /public/$1 [L]
</IfModule>
